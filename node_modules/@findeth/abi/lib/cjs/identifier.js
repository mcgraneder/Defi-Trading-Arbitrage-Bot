"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getIdentifier = exports.parseType = void 0;

var _keccak = require("./utils/keccak256");

const isTuple = input => {
  return input.type === 'tuple';
};

const parseType = input => {
  if (isTuple(input)) {
    return `(${input.components.map(parseType)})`;
  }

  return input.type;
};

exports.parseType = parseType;

const getIdentifier = contractFunction => {
  const types = contractFunction.inputs.map(parseType).join(',');
  return (0, _keccak.keccak256)(`${contractFunction.name}(${types})`).slice(0, 8);
};

exports.getIdentifier = getIdentifier;
//# sourceMappingURL=identifier.js.map