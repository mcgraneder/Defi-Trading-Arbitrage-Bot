/// <reference types="node" />
/**
 * Encode the values with the provided types.
 *
 * @param {Buffer} types
 * @param {unknown[]} values
 */
export declare const encode: (types: string[], values: unknown[]) => Buffer;
